import 'package:flutter/material.dart';

class PizzTab extends StatelessWidget {
  const PizzTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PizzaTab"));
  }
}